---
description: List the configured Postgres database connections
---

Use the `postgres` MCP server's `list_connections` tool and present the configured connections as a markdown list, indicating which one is the default.
