{
  "mcpServers": {
    "postgres": {
      "command": "uv",
      "args": ["run", "--directory", ".", "postgres-mcp", "--access-mode", "restricted"],
      "env": {
        "DATABASE_URI": "${DATABASE_URI:-}"
      }
    }
  }
}
